function Description( {title, description} ){
    return(
        <div style={{marginTop:'5%'}} className="m-5">
            <h2 style={{color:'white'}}>{title}</h2>
            <p style={{color:'white'}}>{description}</p>
        </div>
    )
}

export default Description